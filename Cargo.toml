[package]
name = "hovercraft"
version = "0.1.0"
edition = "2024"

[profile.dev.package."*"]
opt-level = 2

[dependencies]
bevy = "0.18"
rand = "0.10.0"
toml = "1.0.1"
serde = { version = "1.0.228", features = ["derive"] }

[lints.rust]
warnings = "allow"
